# This file is auto-generated by the aliases tool.
# For more info about aliases type `aliases --help`
# or visit https://github.com/sebglazebrook/aliases

start-server:
  command: docker run -ti --name playground-mysql-server -e MYSQL_ROOT_PASSWORD=my-secret-pw --rm -d -v ${PWD}/my.cnf:/root/.my.cnf mysql:latest
start-client:
  command: docker exec -it playground-mysql-server mysql --user=root
cleanup:
  command: docker kill playground-mysql-server
